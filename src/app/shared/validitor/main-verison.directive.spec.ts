import { MainVerisonDirective } from './main-verison.directive';

describe('MainVerisonDirective', () => {
  it('should create an instance', () => {
    const directive = new MainVerisonDirective();
    expect(directive).toBeTruthy();
  });
});
